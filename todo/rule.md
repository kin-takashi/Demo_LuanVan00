# Project Rules (from task instructions)

1. Theo file tree.md này tạo những file còn thiếu, nếu tạo rồi thì bỏ qua.
2. Trong lúc tạo file dùng terminal và chạy code từng lệnh 1 không dùng && hay || để chạy cùng lúc nhiều lệnh.
3. Tạo 1 file tên rule.md ghi lại những luật đã nhắc lần sau không phải nhắc lại.

These rules apply to all future tasks unless updated.
